import React from "react";

const SearchPanel = () => {
  const searchStyle = {
    fontSize: "30px"
  };
  return <input style={searchStyle}
  placeholder="Search" />;
};

export default SearchPanel;
