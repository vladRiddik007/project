import React from 'react';
import TodoListItem from './todo-list-item';

const TodoList = () => {

    return (
        <ul>
            <TodoListItem/>
        </ul>
    );
};
export default TodoList;